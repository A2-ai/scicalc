% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_file_with_hash.R
\name{read_file_with_hash}
\alias{read_file_with_hash}
\title{Read Data File with Hash Verification}
\usage{
read_file_with_hash(
  file_path,
  ...,
  algo = "blake3",
  reader = NULL,
  force = FALSE
)
}
\arguments{
\item{file_path}{path to data file}

\item{...}{additional arguments to digest, read_csv, read_parquet, read_sas, read_pzfx, read_xpt, or \code{reader}}

\item{algo}{hashing algorithm to use, default is "blake3"}

\item{reader}{optional function used to read the file, called as \code{reader(file_path, ...)}.
Required for file extensions this function doesn't know how to read. For a known
extension (csv, parquet, sas7bdat, xpt, pzfx, xlsx/xls/xlsm), \code{reader} is ignored
unless \code{force = TRUE}.}

\item{force}{if \code{TRUE}, use \code{reader} even for a known extension instead of the built-in
reader. Must be \code{FALSE} (the default) when \code{reader} is not supplied.}
}
\value{
data within the supplied file
}
\description{
Read Data File with Hash Verification
}
\examples{
\dontrun{
dat <- read_file_with_hash("data/derived/PK_data.parquet")
dat2 <- read_file_with_hash("data/source/data.csv")
dat3 <- read_file_with_hash("data/derived/pk.feather", reader = arrow::read_feather)
}
}
\seealso{
Other file_io:
\code{\link[=read_hashed_file]{read_hashed_file()}},
\code{\link[=write_file_with_hash]{write_file_with_hash()}}
}
\concept{file_io}
