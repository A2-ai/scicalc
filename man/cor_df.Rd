% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{cor_df}
\alias{cor_df}
\title{Convert Data Frame to Correlation Matrix}
\usage{
cor_df(data, columns = NULL, use = "complete.obs", method = "pearson")
}
\arguments{
\item{data}{A data.frame containing the variables to correlate}

\item{columns}{Character vector of column names to include. If NULL,
all numeric columns will be used.}

\item{use}{Method for handling missing values, passed to cor().
Default is "complete.obs".}

\item{method}{Correlation method, passed to cor(). Default is "pearson".}
}
\value{
A tibble with columns:
\describe{
\item{name1, name2}{Variable pair names (lexicographically ordered)}
\item{CORR}{Correlation coefficient}
\item{ABSCORR}{Absolute correlation coefficient}
}
Results are sorted by ABSCORR in descending order.
}
\description{
Computes pairwise correlations between numeric columns and returns
results in a tidy long format, sorted by absolute correlation.
}
\examples{
# Create sample data
set.seed(123)
df <- data.frame(
  A = rnorm(100, 5, 2),
  B = rnorm(100, 10, 3),
  C = rnorm(100, 15, 1),
  D = letters[1:100] # non-numeric
)
df$B <- df$A * 0.8 + rnorm(100, 0, 1) # Create some correlation

# All numeric columns
cor_df(df)

# Specific columns
cor_df(df, columns = c("A", "B", "C"))

}
\seealso{
Other statistics: 
\code{\link{cv}()},
\code{\link{geom_cv}()},
\code{\link{geom_mean}()},
\code{\link{geom_sd}()}
}
\concept{statistics}
