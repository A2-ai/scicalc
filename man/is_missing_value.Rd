% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_value.R
\name{is_missing_value}
\alias{is_missing_value}
\title{Test for the Missing Value Indicator}
\usage{
is_missing_value(x, missing_value = getOption("scicalc.missing_value", -999))
}
\arguments{
\item{x}{a numeric or \code{units} vector.}

\item{missing_value}{the sentinel to test against. Defaults to
\code{getOption("scicalc.missing_value", -999)}.}
}
\value{
a logical vector the same length as \code{x}.
}
\description{
Returns \code{TRUE} for elements equal to the missing value indicator
(\code{getOption("scicalc.missing_value")}, default \code{-999}). Works whether or not
\code{x} carries units, so it can be used to filter columns that
\code{\link[=convert_units_to_spec]{convert_units_to_spec()}} or \code{\link[=pivot_with_units]{pivot_with_units()}} have made into \code{units}
objects, e.g. \code{dplyr::filter(df, !is_missing_value(NTFD))}.

\code{NA} elements return \code{FALSE} (they are a separate representation of missing,
not the sentinel).
}
\examples{
is_missing_value(c(1, -999, 3))

x <- units::set_units(c(1, -999, 3), "d", mode = "standard")
is_missing_value(x)
}
