% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_units_to_spec.R
\name{convert_units_to_spec}
\alias{convert_units_to_spec}
\alias{convert_units_to_spec.yspec}
\alias{convert_units_to_spec.default}
\title{Convert Data Frame Columns to Spec Units}
\usage{
convert_units_to_spec(data, spec, ...)

\method{convert_units_to_spec}{yspec}(data, spec, ...)

\method{convert_units_to_spec}{default}(data, spec, ...)
}
\arguments{
\item{data}{a data frame.}

\item{spec}{a data specification object; currently a \code{yspec} object.}

\item{...}{reserved for methods.}
}
\value{
\code{data} with columns converted (values rescaled) or assigned units
per the spec. Columns not in the spec, or with no unit in the spec, are
returned untouched.
}
\description{
Converts each unit-carrying column of \code{data} to the unit declared for it in
a data specification. Columns that are plain numeric get the spec unit
attached (assuming the values are already in that unit) with a warning.
Columns whose current units cannot be converted to the spec unit are left
untouched and reported together in a warning.

Pairs with \code{\link[=pivot_with_units]{pivot_with_units()}}: pivot attaches source units, then
\code{convert_units_to_spec()} harmonizes them to the specification.
}
\examples{
\dontrun{
spec <- yspec::ys_load("analysis.yml")
df <- convert_units_to_spec(df, spec)
}
}
\seealso{
Other unit_checking: 
\code{\link{check_for_unique_units}()},
\code{\link{get_unique_units_df}()},
\code{\link{pivot_with_units}()},
\code{\link{with_units}()}
}
\concept{unit_checking}
