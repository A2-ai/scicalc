% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_script}
\alias{audit_script}
\title{Audit an Assembly Script}
\usage{
audit_script(
  script,
  name = NULL,
  dir = default_audit_dir(),
  data = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{script}{path to the assembly script (\code{.R}, \code{.qmd}, or \code{.Rmd}).}

\item{name}{audit name; the log is written to \verb{<dir>/<name>.audit.log}.
Defaults to the script's base name.}

\item{dir}{directory for the named audit log (default: \code{.scicalc-logs} at the project root).}

\item{data}{named final data frame being audited. Its column names seed
static lineage capture in the calling session; the data itself is not sent
to the fresh audit process. Omit to capture runtime evidence only.}

\item{overwrite}{if \code{FALSE} (default), error rather than replace an existing
audit log of the same name. Pass \code{TRUE} to re-run and replace it.}

\item{quiet}{if \code{TRUE}, suppress the assembly script's console output. The
error message on a failed run still includes the script's error.}
}
\value{
the audit as a tibble (invisibly), read from the named log.
}
\description{
Re-runs a data-assembly script in a fresh subprocess with audit capture
enabled, recording a single clean provenance run (input/spec/output hashes
and every unit conversion) to a named log file. Quarto/R Markdown files are
converted to R with \code{\link[knitr:knit]{knitr::purl()}} first, then run with \code{\link[callr:rscript]{callr::rscript()}}.
}
\details{
Because capture is keyed to the \code{SCICALC_AUDITING} environment variable,
scicalc functions log only during this run — normal interactive work logs
nothing — and a nested \code{audit_script()} call inside the script is a no-op, so
a self-auditing script cannot recurse.
}
\examples{
\dontrun{
audit_script("assembly.qmd", name = "pk")
scicalc_audit("pk")
}
}
\seealso{
Other audit: 
\code{\link{scicalc_audit}()},
\code{\link{scicalc_audit_reset}()}
}
\concept{audit}
