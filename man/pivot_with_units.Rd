% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot_with_units.R
\name{pivot_with_units}
\alias{pivot_with_units}
\title{Pivot Wider and Attach Units}
\usage{
pivot_with_units(data, values_from, units_from, names_from, ...)
}
\arguments{
\item{data}{a long-format data frame.}

\item{values_from}{column holding the values to spread (as in
\code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}}).}

\item{units_from}{column holding the unit associated with each value.}

\item{names_from}{column whose values become the new column names.}

\item{...}{additional arguments passed to \code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}}.}
}
\value{
a wide data frame with a \code{units} object attached to each pivoted
column that has a parseable unit.
}
\description{
A wrapper around \code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}} that additionally attaches a \code{units}
object to each pivoted column, taken from a units column in the long data.

Each value in \code{names_from} must map to a single unit in \code{units_from}
(after normalizing \code{IU} to \code{U} and \code{μ} to \code{u}); otherwise an error is
raised, since a column cannot carry more than one unit. Columns whose unit is
missing or empty are returned as plain numeric (no unit attached).
}
\examples{
df <- data.frame(
  ID = c(1, 1, 2, 2),
  TEST = c("ALT", "AST", "ALT", "AST"),
  VAL = c(20, 30, 25, 35),
  UNIT = c("U/L", "U/L", "U/L", "U/L")
)
pivot_with_units(df, values_from = VAL, units_from = UNIT, names_from = TEST)
}
\seealso{
Other unit_checking: 
\code{\link{check_for_unique_units}()},
\code{\link{convert_units_to_spec}()},
\code{\link{get_unique_units_df}()},
\code{\link{with_units}()}
}
\concept{unit_checking}
